name: dotnet package

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        dotnet-version: [ '6.0.x' ]

    steps:
      - uses: actions/checkout@v3
      - name: Setup .NET Core SDK ${{ matrix.dotnet-version }}
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: ${{ matrix.dotnet-version }}
        env:
          ConnectionStrings__PepitesDatabase: ${{secrets.ConnectionStrings__PepitesDatabase}}
      - name: Install dependencies
        run: dotnet restore
#      - name: Build
#        run: dotnet build ./DbMigration/DbMigration.csproj Release
      - name: Run
        run: dotnet publish ./DbMigration/DbMigration.csproj -c Release -o out
      - name: Run
        run: dotnet ./out/DbMigration.dll
        
        #dotnet publish ./src/Effy.CustomerReferential.DbMigration/Effy.CustomerReferential.DbMigration.csproj --no-restore -c Release -o out

